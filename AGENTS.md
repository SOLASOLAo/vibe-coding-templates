# AGENTS.md — <项目名> 协作约定

> 初始化时将占位符替换为项目事实，删除不适用的条目。当前进度记录在 HANDOVER.md。

## 项目与环境

<解决什么问题 / 面向谁 / 技术栈及必须遵守的架构约束>

| 用途 | 命令或事实 |
| --- | --- |
| 语言 / 运行时 | <版本> |
| 安装依赖 | <命令> |
| 启动 | <命令> |
| 构建 | <命令> |
| 相关测试 | <命令与适用范围> |

## 按任务读取资料

- README.md：了解项目、启动方式和对外行为。
- HANDOVER.md：恢复未完成工作，或核对当前状态与阻塞。
- TODO.md：选择待办，查看本次任务的范围和验收标准。
- docs/：按需查阅相关设计、环境说明和验证证据；不要求每次完整阅读。

## 执行与边界

- 用户决定目标、范围和关键取舍；AI 在既定约束及已有授权内完成常规设计、实现、调试和验证。
- 编码任务持续推进到约定验收条件，包括需要的运行、检查和修复。不要仅完成第一版实现便停止；也不要把单次任务扩展为未请求的长期工作。
- 已有授权持续有效，常规可逆步骤无需逐次确认。只有影响结果的必要信息缺失、超出授权或真实外部阻塞时，说明具体需要；同时继续可独立完成的工作。
- 项目特有边界：<真实设备 / 生产环境 / 付费操作 / 许可证 / 用户数据的具体限制；不适用则删除>。
- 保护用户数据、凭据和已有改动；先检查受影响内容，遵守适用的备份与兼容要求。不要覆盖他人或先前任务的未提交工作。

## 验证与交付

- 验证范围与改动匹配，完成相关检查。检查通过后，仅在新改动、失败或未解决问题出现时扩大或重复验证。
- 区分静态审查、编译、自动化测试、实际运行和性能测量；准确报告执行过的检查、结果及未验证内容。
- 缺少依赖、硬件或许可时记录具体阻塞及其影响；条件没有变化时，不反复执行同一失败检查。

## 文档与 Git

- 目标或使用方式变化时更新 README；状态、决策、阻塞或后续工作变化时更新 HANDOVER / TODO；技术结论和验证证据放入 docs。
- AGENTS 只保留长期适用的规则、环境事实及资料入口，不重复维护进度快照。纯问答或无状态变化的小任务无需改写交接文件。
- Git 操作遵循用户已有授权和项目约定。提交前检查差异，只纳入本任务已核对的内容；前缀使用 feat、fix、docs、test、tools 或 refactor。
- 仅向明确的项目远程与分支推送；模板来源链接不等于项目远程。没有变更、没有远程或未获推送授权时，不因会话结束而提交推送。

## 可读性与模块化原则

**1. Codex 可读性：让人和 Codex 都能快速定位、理解和验证代码。**

- 使用明确、统一的功能命名；入口、调用关系和状态流转应可直接追踪，不依赖历史对话才能理解。
- 对外接口说明输入输出、单位（如适用）、有效条件、完成条件和错误语义；复杂逻辑的注释解释原因与约束，不重复代码字面意思。
- 保持源码可搜索；工程容器或生成工具内的代码按需保留与工程同步的可读快照。关键逻辑关联对应规范依据和测试入口，文档维护唯一事实源，避免多份说明互相矛盾。

**2. 功能模块化设计：按职责和变化边界拆分，不为拆分而拆分。**

- 按实际职责区分数据编解码、外部交互、业务规则、流程编排和界面展示；业务规则由所属模块维护，避免多处重复实现。
- 模块通过明确的公开接口交互，明确状态所有者；不得跨模块直接修改内部状态。异步或有状态接口需明确触发、忙碌、完成、失败、取消及复位语义（如适用）。
- 优先复用标准库、平台能力和现有实现；只有独立职责、实际复用或独立验证需求时才新增模块。不强制文件行数，不预建通用框架，不拆成大量薄包装。

### 适用范围

- 两项原则用于后续新增和实际触及的修改，不要求一次性整理整个项目。
- 保持既有工具与人工/AI 所有权边界及公开接口兼容约束。
